import React from 'react';

import styles from './Header.scss';

const Header = () => (
  <div className={styles.header}>
    <h1 className={styles.title}>Simulateur emprunt immobilier</h1>
  </div>
);

export default Header;
